/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ResortTemperatureComponent } from './resort-temperature.component';

describe('Component: ResortTemperature', () => {
  it('should create an instance', () => {
    let component = new ResortTemperatureComponent();
    expect(component).toBeTruthy();
  });
});
